[project]
name = "powerplatform-python-migration"
version = "0.1.0"
description = "Configurable starter for migrating Power Platform solution exports to Python."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]