[project]
name = "cchair-ai"
version = "1.0.0"
description = "CoChairAI: Meeting governance workspace for planning, minutes, and action tracking."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]